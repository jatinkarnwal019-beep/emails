{
  "name": "outreach-backend",
  "version": "1.0.0",
  "description": "Outreach agent backend — sends emails via Resend",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.18.2"
  },
  "engines": {
    "node": ">=18"
  }
}
